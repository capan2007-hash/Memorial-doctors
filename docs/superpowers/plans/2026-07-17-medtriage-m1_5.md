# MedTriage M1.5 — Talep & Doktor Kartı Zenginleştirme Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Talep girişini (demografi + tıbbi geçmiş + diş röntgeni) ve doktor profil kartını (çoklu yetkinlik + CV + foto + ağırlıklı işler + performans paneli + giriş hesabı) BRD'ye uydurmak; doktor & satışçı görünümlerini zenginleştirmek; rol-farkında navigasyon eklemek.

**Architecture:** M1 üstüne ek. Kritik model değişikliği: doktor tek `subcategory_id` yerine `doctor_scope` many-to-many ile birden çok (kategori, alt-kırılım) yetkinliğine sahip; `resolveAssignees` saf fonksiyonu buna göre yeniden yazılır (TDD). Yeni talep alanları `request` tablosunda snapshot. Doktor giriş hesabı bir `create-doctor` Edge Function (service role) ile davet-bazlı açılır.

**Tech Stack:** React 18 + Vite + TS + Tailwind, Supabase (Postgres+RLS+Storage+Auth+Edge Functions/Deno), Vitest, Playwright, @anthropic yok (bu turda AI yok — M2).

## Global Constraints

- Arayüz Türkçe; kod/identifier İngilizce.
- Multi-tenant: her yeni tablo `tenant_id`; RLS zorunlu; `current_tenant_id()` deseni M1'den.
- İzin sınırı (M1): doktor yanıtı/tedavi planı aracıya (agent) asla görünmez — bu turda korunur.
- Rule=Data: atama mantığı `resolveAssignees` domain fonksiyonunda; koda gömülü değil.
- Zorunlu talep alanları: yaş, kilo, boy, cinsiyet. Tıbbi alanlar (geçmiş ameliyat/hastalık/ilaç) "Yok" ya da metin — boş bırakılamaz.
- Doktor çoklu yetkinlik: `doctor_scope` (doctor_id, category_id, subcategory_id?). Atama scope eşleşmesiyle.
- Hesap oluşturma yalnız koordinatör/admin (davet-bazlı, self-signup yok).
- Commit dili: Türkçe conventional; her commit sonunda `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## Dosya Yapısı

```
supabase/migrations/
  0007_enrichment.sql          # request alanları, photo.kind, doctor_scope + backfill
supabase/functions/
  create-doctor/index.ts       # Edge Function: auth user + app_user + doctor + scope (service role)
src/
  domain/
    health.ts                  # bmi(), medical alan doğrulama (saf)
    assignment.ts              # resolveAssignees — scope tabanlı (YENİDEN YAZILIR)
    __tests__/health.test.ts
    __tests__/assignment.test.ts   # yeniden yazılır
  types/db.ts                  # yeni kolonlar + DoctorScopeRow + Gender/PhotoKind tipleri
  lib/nav.ts                   # role → menü linkleri (saf tablo)
  components/Layout.tsx        # rol-farkında menü (MODIFY)
  features/
    catalog/useCatalog.ts      # (varsa) alt kırılım okuma zaten var
    requests/
      NewRequestWizard.tsx     # demografi + gender + tıbbi + diş röntgeni (MODIFY)
      usePhotoUpload.ts        # kind parametresi (MODIFY)
      useRequests.ts           # yeni alanlar + scope tabanlı atama (MODIFY)
      RequestDetail.tsx        # hasta bilgisi + fotoğraflar (MODIFY)
      PatientInfoCard.tsx      # paylaşılan hasta-bilgi gösterim bileşeni (NEW)
    doctor/
      DoctorRequestView.tsx    # PatientInfoCard + foto/röntgen (MODIFY)
    admin/
      DoctorAdmin.tsx          # zengin kart: foto/CV/yetkinlik/ağırlıklı iş/metrik/düzenle (MODIFY, büyür)
      useDoctors.ts            # doktor CRUD + scope + metrik hook'ları (NEW)
      AllRequests.tsx          # "Doktor atanmadı" rozeti (MODIFY)
```

---

## Task 1: Domain — `health.ts` (bmi + tıbbi alan doğrulama)

**Files:**
- Create: `src/domain/health.ts`, `src/domain/__tests__/health.test.ts`

**Interfaces:**
- Produces:
  - `function bmi(weightKg: number, heightCm: number): number | null` — geçersiz girdide null; aksi 1 ondalık yuvarlanmış BMI.
  - `function medicalValue(none: boolean, text: string): string | null` — none=true → 'Yok'; none=false && text boş → null (geçersiz); aksi text.trim().

- [ ] **Step 1: Failing test yaz** — `src/domain/__tests__/health.test.ts`

```ts
import { describe, it, expect } from 'vitest'
import { bmi, medicalValue } from '../health'

describe('bmi', () => {
  it('normal hesap (70kg, 175cm ≈ 22.9)', () => { expect(bmi(70, 175)).toBe(22.9) })
  it('geçersiz girdi null', () => {
    expect(bmi(0, 175)).toBeNull()
    expect(bmi(70, 0)).toBeNull()
    expect(bmi(-5, 175)).toBeNull()
  })
})

describe('medicalValue', () => {
  it('yok işaretliyse "Yok"', () => { expect(medicalValue(true, '')).toBe('Yok') })
  it('yok değilse metin döner', () => { expect(medicalValue(false, '  aspirin ')).toBe('aspirin') })
  it('yok değil ve boşsa null (geçersiz)', () => { expect(medicalValue(false, '   ')).toBeNull() })
})
```

- [ ] **Step 2: Testi çalıştır, fail gör**

Run: `npx vitest run src/domain/__tests__/health.test.ts`
Expected: FAIL ("Cannot find module '../health'")

- [ ] **Step 3: Implementasyon** — `src/domain/health.ts`

```ts
export function bmi(weightKg: number, heightCm: number): number | null {
  if (!(weightKg > 0) || !(heightCm > 0)) return null
  const m = heightCm / 100
  return Math.round((weightKg / (m * m)) * 10) / 10
}

export function medicalValue(none: boolean, text: string): string | null {
  if (none) return 'Yok'
  const t = text.trim()
  return t.length ? t : null
}
```

- [ ] **Step 4: Testi çalıştır, pass gör**

Run: `npx vitest run src/domain/__tests__/health.test.ts`
Expected: PASS (5 test)

- [ ] **Step 5: Commit**

```bash
git add src/domain/health.ts src/domain/__tests__/health.test.ts
git commit -m "feat: bmi + tıbbi alan doğrulama domain yardımcıları

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Domain — `resolveAssignees` çoklu-yetkinlik (scope) yeniden yazımı

**Files:**
- Modify: `src/domain/assignment.ts` (tamamen yeniden)
- Modify: `src/domain/__tests__/assignment.test.ts` (tamamen yeniden)

**Interfaces:**
- Produces:
  - `interface DoctorScope { categoryId: string; subcategoryId: string | null }`
  - `interface ScopedDoctor { id: string; isActive: boolean; scopes: DoctorScope[] }`
  - `interface AssignmentTarget { categoryId: string; subcategoryId: string | null }`
  - `function resolveAssignees(target: AssignmentTarget, doctors: ScopedDoctor[]): string[]`
  - Eşleşme: doktor aktif ve **bir** scope'u hedefe uyuyor: `scope.categoryId === target.categoryId && (target.subcategoryId == null ? scope.subcategoryId == null : scope.subcategoryId === target.subcategoryId)`.

- [ ] **Step 1: Testi yeniden yaz** — `src/domain/__tests__/assignment.test.ts`

```ts
import { describe, it, expect } from 'vitest'
import { resolveAssignees, type ScopedDoctor } from '../assignment'

const docs: ScopedDoctor[] = [
  // Saç Ekimi (alt-kırılımsız) doktoru
  { id: 'd1', isActive: true, scopes: [{ categoryId: 'sac', subcategoryId: null }] },
  { id: 'd2', isActive: false, scopes: [{ categoryId: 'sac', subcategoryId: null }] },
  // Plastik cerrah: meme + vücut + yüz yapar, burun YAPMAZ
  { id: 'd3', isActive: true, scopes: [
    { categoryId: 'plastik', subcategoryId: 'meme' },
    { categoryId: 'plastik', subcategoryId: 'vucut' },
    { categoryId: 'plastik', subcategoryId: 'yuz' },
  ] },
  // Sadece burun yapan doktor
  { id: 'd4', isActive: true, scopes: [{ categoryId: 'plastik', subcategoryId: 'burun' }] },
]

describe('resolveAssignees (scope)', () => {
  it('alt-kırılımsız kategoride null-eşleşen aktif doktorlar', () => {
    expect(resolveAssignees({ categoryId: 'sac', subcategoryId: null }, docs)).toEqual(['d1'])
  })
  it('pasif doktor atanmaz', () => {
    expect(resolveAssignees({ categoryId: 'sac', subcategoryId: null }, docs)).not.toContain('d2')
  })
  it('Meme talebi: meme yapan plastik cerraha düşer, buruncuya düşmez', () => {
    expect(resolveAssignees({ categoryId: 'plastik', subcategoryId: 'meme' }, docs).sort()).toEqual(['d3'])
  })
  it('Burun talebi: sadece burun yapan doktora düşer (d3 düşmez)', () => {
    expect(resolveAssignees({ categoryId: 'plastik', subcategoryId: 'burun' }, docs)).toEqual(['d4'])
  })
  it('Yüz talebi: d3 düşer', () => {
    expect(resolveAssignees({ categoryId: 'plastik', subcategoryId: 'yuz' }, docs)).toEqual(['d3'])
  })
  it('eşleşme yoksa boş', () => {
    expect(resolveAssignees({ categoryId: 'plastik', subcategoryId: 'genital' }, docs)).toEqual([])
  })
})
```

- [ ] **Step 2: Testi çalıştır, fail gör**

Run: `npx vitest run src/domain/__tests__/assignment.test.ts`
Expected: FAIL (eski `AssignableDoctor` imzası; `ScopedDoctor` yok)

- [ ] **Step 3: `assignment.ts`'i yeniden yaz**

```ts
export interface DoctorScope {
  categoryId: string
  subcategoryId: string | null
}
export interface ScopedDoctor {
  id: string
  isActive: boolean
  scopes: DoctorScope[]
}
export interface AssignmentTarget {
  categoryId: string
  subcategoryId: string | null
}

function scopeMatches(scope: DoctorScope, target: AssignmentTarget): boolean {
  if (scope.categoryId !== target.categoryId) return false
  return target.subcategoryId == null
    ? scope.subcategoryId == null
    : scope.subcategoryId === target.subcategoryId
}

export function resolveAssignees(target: AssignmentTarget, doctors: ScopedDoctor[]): string[] {
  return doctors
    .filter((d) => d.isActive && d.scopes.some((s) => scopeMatches(s, target)))
    .map((d) => d.id)
}
```

- [ ] **Step 4: Testi çalıştır, pass gör**

Run: `npx vitest run src/domain/__tests__/assignment.test.ts`
Expected: PASS (6 test)

- [ ] **Step 5: Commit**

```bash
git add src/domain/assignment.ts src/domain/__tests__/assignment.test.ts
git commit -m "feat: resolveAssignees çoklu-yetkinlik (doctor_scope) modeline geçiş

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Şema migration (request alanları + photo.kind + doctor_scope + backfill)

> **NOT (controller):** Bu görev Supabase'e (proje `oxibdniwobetaksuxacs`) migration uygular. Controller MCP `apply_migration`/`execute_sql` ile yürütür.

**Files:**
- Create: `supabase/migrations/0007_enrichment.sql`

**Interfaces:**
- Produces: `request` yeni kolonları, `photo.kind` enum, `doctor_scope` tablosu + RLS + backfill, `gender_type`/`photo_kind` enum'ları.

- [ ] **Step 1: Migration'ı yaz** — `supabase/migrations/0007_enrichment.sql`

```sql
-- Enums
create type gender_type as enum ('female','male','other');
create type photo_kind as enum ('photo','xray');

-- request: demografi + tıbbi snapshot
alter table request add column age int;
alter table request add column weight_kg numeric;
alter table request add column height_cm int;
alter table request add column gender gender_type;
alter table request add column past_surgeries text not null default 'Yok';
alter table request add column known_conditions text not null default 'Yok';
alter table request add column medications text not null default 'Yok';

-- photo: tür (foto | röntgen)
alter table photo add column kind photo_kind not null default 'photo';

-- doctor_scope: çoklu yetkinlik (kategori/alt-kırılım)
create table doctor_scope (
  id uuid primary key default gen_random_uuid(),
  tenant_id uuid not null references tenant(id),
  doctor_id uuid not null references doctor(id) on delete cascade,
  category_id uuid not null references category(id),
  subcategory_id uuid references subcategory(id),
  unique (doctor_id, category_id, subcategory_id)
);
create index on doctor_scope (doctor_id);
create index on doctor_scope (category_id, subcategory_id);

alter table doctor_scope enable row level security;
create policy scope_tenant_read on doctor_scope for select using (tenant_id = current_tenant_id());
create policy scope_admin_all on doctor_scope for all
  using (tenant_id = current_tenant_id() and current_role_name() in ('coordinator','admin'))
  with check (tenant_id = current_tenant_id());

-- Backfill: mevcut doktorların category/subcategory'sini scope'a taşı
insert into doctor_scope (tenant_id, doctor_id, category_id, subcategory_id)
select tenant_id, id, category_id, subcategory_id from doctor
on conflict do nothing;
```

- [ ] **Step 2: Uygula (controller, MCP)**

`apply_migration(project_id="oxibdniwobetaksuxacs", name="0007_enrichment", query=<yukarıdaki>)`

- [ ] **Step 3: Doğrula**

`execute_sql`: `select count(*) from doctor_scope;` → mevcut doktor sayısı (2) kadar satır.
`get_advisors(type="security")` → `doctor_scope` için RLS uyarısı OLMAMALI.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0007_enrichment.sql
git commit -m "feat: request demografi/tıbbi alanları, photo.kind, doctor_scope + backfill

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: db.ts tipleri + create-flow (yeni alanlar + scope tabanlı atama)

**Files:**
- Modify: `src/types/db.ts`, `src/features/requests/usePhotoUpload.ts`, `src/features/requests/useRequests.ts`, `src/features/admin/AllRequests.tsx` (reassign)

**Interfaces:**
- Consumes: `resolveAssignees(target, ScopedDoctor[])` (Task 2)
- Produces: genişletilmiş `NewRequestInput` (demografi + gender + tıbbi + xrayFiles), scope tabanlı atama.

- [ ] **Step 1: db.ts'e tip ekle** — `src/types/db.ts`

`RequestRow` interface'ine ekle: `age: number | null; weight_kg: number | null; height_cm: number | null; gender: 'female'|'male'|'other'|null; past_surgeries: string; known_conditions: string; medications: string`.
`PhotoRow`'a ekle: `kind: 'photo'|'xray'`.
Yeni tip: `export interface DoctorScopeRow { id: string; tenant_id: string; doctor_id: string; category_id: string; subcategory_id: string | null }`.

- [ ] **Step 2: usePhotoUpload kind desteği** — `src/features/requests/usePhotoUpload.ts`

```ts
import { supabase } from '../../lib/supabase'

export async function uploadPhotos(
  tenantId: string, requestId: string, files: File[], kind: 'photo' | 'xray' = 'photo',
) {
  for (const file of files) {
    const path = `${tenantId}/${requestId}/${crypto.randomUUID()}-${file.name}`
    const { error: upErr } = await supabase.storage.from('photos').upload(path, file)
    if (upErr) throw upErr
    const { error: insErr } = await supabase.from('photo').insert({
      tenant_id: tenantId, request_id: requestId, storage_path: path, kind,
    })
    if (insErr) throw insErr
  }
}
```

- [ ] **Step 3: useRequests — yeni alanlar + scope atama** — `src/features/requests/useRequests.ts`

`NewRequestInput`'u genişlet ve create-flow'u scope'a çevir:
```ts
interface NewRequestInput {
  tenantId: string
  createdBy: string
  patient: { first_name: string; last_name: string; phone?: string }
  age: number; weightKg: number; heightCm: number; gender: 'female'|'male'|'other'
  pastSurgeries: string; knownConditions: string; medications: string
  categoryId: string
  subcategoryId: string | null
  operationTypeId: string | null
  notes?: string
  files: File[]
  xrayFiles?: File[]
}
```
`request.insert`'e ekle: `age, weight_kg: weightKg, height_cm: heightCm, gender, past_surgeries: pastSurgeries, known_conditions: knownConditions, medications`.
Fotoğraflar sonrası röntgenler: `if (input.xrayFiles?.length) await uploadPhotos(input.tenantId, req.id, input.xrayFiles, 'xray')`.
Atama scope tabanlı:
```ts
// doktorları scope'larıyla getir
const { data: docs } = await supabase.from('doctor').select('id, is_active').eq('tenant_id', input.tenantId)
const { data: scopes } = await supabase.from('doctor_scope').select('doctor_id, category_id, subcategory_id').eq('tenant_id', input.tenantId)
const scoped = (docs ?? []).map((d: any) => ({
  id: d.id, isActive: d.is_active,
  scopes: (scopes ?? []).filter((s: any) => s.doctor_id === d.id)
    .map((s: any) => ({ categoryId: s.category_id, subcategoryId: s.subcategory_id })),
}))
const targets = resolveAssignees({ categoryId: input.categoryId, subcategoryId: input.subcategoryId }, scoped)
```
`DoctorRow` importu yerine `ScopedDoctor` kullanılır (satır 5 importunu güncelle).

- [ ] **Step 4: AllRequests reassign'i scope'a çevir** — `src/features/admin/AllRequests.tsx`

Reassign mutation'ında doktorları `doctor` + `doctor_scope`'tan çekip `resolveAssignees({categoryId: req.category_id, subcategoryId: req.subcategory_id}, scoped)` ile hedefleri hesapla (Task 9 useRequests deseniyle aynı). Sadece hedeflenen doktorlara upsert.

- [ ] **Step 5: Derleme + testler**

Run: `npx tsc --noEmit -p tsconfig.app.json` → temiz
Run: `npx vitest run` → tüm domain testleri geçer

- [ ] **Step 6: Commit**

```bash
git add src/types/db.ts src/features/requests/usePhotoUpload.ts src/features/requests/useRequests.ts src/features/admin/AllRequests.tsx
git commit -m "feat: talep yeni alanları + röntgen yükleme + scope tabanlı atama (create+reassign)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: NewRequestWizard — demografi + cinsiyet + tıbbi (Yok) + diş röntgeni

**Files:**
- Modify: `src/features/requests/NewRequestWizard.tsx`

**Interfaces:**
- Consumes: `useCreateRequest` (genişletilmiş input), `medicalValue`/`bmi` (health.ts), `PhotoUploader`.

- [ ] **Step 1: Alanları ve doğrulamayı ekle**

State ekle: `age, weightKg, heightCm` (string→number), `gender`, üç tıbbi alan için `{none:boolean, text:string}`, `xrayFiles: File[]`.
Formda: Ad, Soyad (mevcut) + **Yaş / Boy(cm) / Kilo(kg)** number input + **Cinsiyet** select (Kadın/Erkek/Diğer) + kategori/alt-kırılım/operasyon (mevcut) + üç tıbbi alan (her biri: "Yok" checkbox + checkbox kapalıysa textarea) + not + **PhotoUploader (fotoğraflar)** + **kategori = Diş Tedavisi ise** ek "Diş röntgeni (opsiyonel)" PhotoUploader (`xrayFiles`).
`canSubmit`: first && last && age>0 && weightKg>0 && heightCm>0 && gender && needsSub? subcategoryId : true && her tıbbi alan (none || text.trim()) && files.length>0.
Submit'te `medicalValue(none,text)` ile üç değeri hesapla; mutation'a yeni input geç. Diş değilse xrayFiles boş.
Diş Tedavisi kategori id'si: seçili kategori adının 'Diş' içermesiyle veya `category.name === 'Diş Tedavisi'` ile belirlenir (kategori listesinden).

- [ ] **Step 2: Derle + manuel doğrula**

Run: `npx tsc --noEmit -p tsconfig.app.json` → temiz. `npm run build` → temiz.
Manuel: satışçı ile Diş Tedavisi seç → röntgen yükleyici görünür; başka kategoride görünmez; tıbbi alanda "Yok" işaretlemeden metin girmezse Gönder pasif.

- [ ] **Step 3: Commit**

```bash
git add src/features/requests/NewRequestWizard.tsx
git commit -m "feat: talep formu — yaş/boy/kilo/cinsiyet + tıbbi (Yok) + diş röntgeni

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: Layout — rol-farkında navigasyon menüsü

**Files:**
- Create: `src/lib/nav.ts`
- Modify: `src/components/Layout.tsx`

**Interfaces:**
- Produces: `navLinks(role): {to, label}[]`

- [ ] **Step 1: nav tablosu** — `src/lib/nav.ts`

```ts
import type { Role } from '../types/domain'

export interface NavLink { to: string; label: string }

export function navLinks(role: Role | null): NavLink[] {
  switch (role) {
    case 'sales':
    case 'agent':
      return [{ to: '/requests', label: 'Talepler' }, { to: '/requests/new', label: 'Yeni Talep' }]
    case 'doctor':
      return [{ to: '/doctor', label: 'Bekleyen Talepler' }]
    case 'coordinator':
    case 'admin':
      return [{ to: '/admin/requests', label: 'Tüm Talepler' }, { to: '/admin/doctors', label: 'Doktor Yönetimi' }]
    default:
      return []
  }
}
```

- [ ] **Step 2: Layout'a menü ekle** — `src/components/Layout.tsx`

Header'a rol linklerini render et (`useAuth().role` → `navLinks`), `react-router-dom` `Link` ile:
```tsx
import { Link } from 'react-router-dom'
import { navLinks } from '../lib/nav'
// header içinde app adı ile kullanıcı arasında:
<nav className="flex gap-4 text-sm">
  {navLinks(role).map((l) => <Link key={l.to} to={l.to} className="hover:underline">{l.label}</Link>)}
</nav>
```
`role`'ü `useAuth()`'tan al.

- [ ] **Step 3: Derle + manuel**

`npm run build` temiz. Koordinatör ile giriş → üst barda "Tüm Talepler" ve "Doktor Yönetimi" linkleri görünür ve çalışır.

- [ ] **Step 4: Commit**

```bash
git add src/lib/nav.ts src/components/Layout.tsx
git commit -m "feat: rol-farkında navigasyon menüsü (Layout)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: DoctorAdmin — zengin doktor kartı (foto/CV/yetkinlik/ağırlıklı iş/düzenle) + metrik paneli

**Files:**
- Create: `src/features/admin/useDoctors.ts`
- Modify: `src/features/admin/DoctorAdmin.tsx`

**Interfaces:**
- Consumes: `useCategories`, `useSubcategories`, `supabase`, `useAuth`
- Produces: doktor listeleme/oluşturma (create-doctor invoke) / güncelleme, scope düzenleme, metrik hesaplama.

- [ ] **Step 1: useDoctors hook'ları** — `src/features/admin/useDoctors.ts`

- `useDoctorsFull()` → doktorlar + scope'ları.
- `useDoctorMetrics(doctorId)` → `{acceptCount, rejectCount, avgResponseMins}`:
  - accept/reject: `response` count (decision, doctor_id).
  - avgResponseMins: `response.responded_at` − ilgili `assignment.assigned_at` ortalaması (dakika). Sorgu: response join assignment on (request_id, doctor_id).
- `useUpdateDoctor()` → doctor profil güncelle (specialty, bio, weighted_work, is_active, photo_url) + scope satırlarını yeniden yaz (sil/ekle) + `audit_log`'a yaz.
- `useCreateDoctor()` → `supabase.functions.invoke('create-doctor', { body: {...} })` (Task 8). Dönerse listeyi invalidate et.

- [ ] **Step 2: DoctorAdmin ekranını genişlet**

- **Yeni doktor formu:** e-posta + geçici şifre + ad + unvan + branş + biyografi + kategori seçimi + o kategorinin alt kırılımları (çoklu checkbox) + ağırlıklı işler (satır ekle: alan+seviye) + foto (opsiyonel) → `create-doctor` invoke.
- **Doktor listesi:** her doktor kartı açılır/düzenlenir; yetkinlikler (scope) checkbox'larla değiştirilir; kaydet → useUpdateDoctor.
- **Metrik paneli:** kartın altında `useDoctorMetrics` sonuçları (kabul/red/ort. süre/skor) salt-okunur; veri yoksa 0/"—".
- Foto yükleme: `photos` bucket'ında `${tenantId}/doctors/${doctorId}/...` yoluna yükle, `doctor.photo_url` = imzalı/again signed on read. (Basitlik: storage_path sakla, gösterimde signed URL üret.)

- [ ] **Step 3: Derle + manuel doğrula**

`npm run build` temiz. (Canlı create-doctor testi Task 8 sonrası controller ile.)

- [ ] **Step 4: Commit**

```bash
git add src/features/admin/useDoctors.ts src/features/admin/DoctorAdmin.tsx
git commit -m "feat: zengin doktor kartı — foto/CV/çoklu yetkinlik/ağırlıklı iş/metrik + düzenleme

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: create-doctor Edge Function (davet-bazlı hesap oluşturma)

> **NOT (controller):** Edge Function deploy + secret gerektirir. Controller MCP `deploy_edge_function` ile deploy eder. Fonksiyon `SUPABASE_SERVICE_ROLE_KEY` ve `SUPABASE_URL` ortam değişkenlerini (Supabase edge runtime otomatik sağlar) kullanır.

**Files:**
- Create: `supabase/functions/create-doctor/index.ts`

**Interfaces:**
- Consumes: çağıranın JWT'si (Authorization header) → rol doğrulama.
- Produces: `{ doctorId }`. Adımlar: (1) çağıran coordinator/admin mi doğrula; (2) auth user oluştur (email+password, email_confirm=true); (3) `app_user` (role=doctor); (4) `doctor` profili; (5) `doctor_scope` satırları.

- [ ] **Step 1: Fonksiyonu yaz** — `supabase/functions/create-doctor/index.ts`

```ts
import { createClient } from 'npm:@supabase/supabase-js@2'

Deno.serve(async (req) => {
  if (req.method !== 'POST') return new Response('method', { status: 405 })
  const url = Deno.env.get('SUPABASE_URL')!
  const serviceKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  const authHeader = req.headers.get('Authorization') ?? ''

  // Çağıranın kimliği/rolü (anon değil, çağıranın JWT'siyle)
  const caller = createClient(url, Deno.env.get('SUPABASE_ANON_KEY')!, {
    global: { headers: { Authorization: authHeader } },
  })
  const { data: userRes } = await caller.auth.getUser()
  if (!userRes?.user) return json({ error: 'unauthorized' }, 401)
  const { data: me } = await caller.from('app_user').select('tenant_id, role').eq('id', userRes.user.id).single()
  if (!me || !['coordinator', 'admin'].includes(me.role)) return json({ error: 'forbidden' }, 403)

  const body = await req.json()
  const { email, password, fullName, title, specialty, bio, weightedWork, scopes } = body
  if (!email || !password || !fullName) return json({ error: 'missing fields' }, 400)

  const admin = createClient(url, serviceKey)
  // 1) auth user
  const { data: created, error: cErr } = await admin.auth.admin.createUser({
    email, password, email_confirm: true,
  })
  if (cErr) return json({ error: cErr.message }, 400)
  const uid = created.user.id
  // 2) app_user
  const { error: auErr } = await admin.from('app_user').insert({
    id: uid, tenant_id: me.tenant_id, role: 'doctor', full_name: fullName,
  })
  if (auErr) return json({ error: auErr.message }, 400)
  // 3) doctor
  const { data: doc, error: dErr } = await admin.from('doctor').insert({
    tenant_id: me.tenant_id, app_user_id: uid, title, specialty, bio,
    weighted_work: weightedWork ?? { items: [], note: '' },
    category_id: (scopes?.[0]?.categoryId) ?? null, is_active: true,
  }).select('id').single()
  if (dErr) return json({ error: dErr.message }, 400)
  // 4) scopes
  if (Array.isArray(scopes) && scopes.length) {
    const rows = scopes.map((s: any) => ({
      tenant_id: me.tenant_id, doctor_id: doc.id,
      category_id: s.categoryId, subcategory_id: s.subcategoryId ?? null,
    }))
    const { error: sErr } = await admin.from('doctor_scope').insert(rows)
    if (sErr) return json({ error: sErr.message }, 400)
  }
  return json({ doctorId: doc.id }, 200)
})

function json(o: unknown, status: number) {
  return new Response(JSON.stringify(o), { status, headers: { 'Content-Type': 'application/json' } })
}
```
Not: `doctor.category_id` NOT NULL ise scope[0].categoryId'yi birincil kategori olarak yazıyoruz; kayıt scope tablosu üzerinden çalışır.

- [ ] **Step 2: Deploy (controller, MCP)**

`deploy_edge_function(project_id="oxibdniwobetaksuxacs", name="create-doctor", files=[{name:"index.ts", content:<yukarıdaki>}])`.

- [ ] **Step 3: Canlı doğrula (controller)**

Koordinatör JWT ile invoke → yeni doktor + app_user + doctor + scope oluşur; yeni e-posta/şifreyle giriş yapılıp doktor kuyruğu görülür. Yetkisiz (agent) çağrı → 403.

- [ ] **Step 4: Commit**

```bash
git add supabase/functions/create-doctor/index.ts
git commit -m "feat: create-doctor Edge Function (davet-bazlı doktor hesabı + scope)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: PatientInfoCard + DoctorRequestView zenginleştirme

**Files:**
- Create: `src/features/requests/PatientInfoCard.tsx`
- Modify: `src/features/doctor/DoctorRequestView.tsx`

**Interfaces:**
- Consumes: `bmi` (health.ts), `RequestRow`, katalog adları.
- Produces: `PatientInfoCard({ req, patientName, categoryName, subcategoryName, operationName })` — hasta bilgisi + demografi + BMI + tıbbi bilgi gösterir.

- [ ] **Step 1: PatientInfoCard** — `src/features/requests/PatientInfoCard.tsx`

`req` (RequestRow) + isim/kategori/operasyon adlarını alıp gösteren salt-okunur bileşen: Hasta adı, Kategori/Alt kırılım, İstenen operasyon, Yaş/Cinsiyet, Boy/Kilo + **BMI** (`bmi(weight_kg, height_cm)`), Geçmiş ameliyatlar, Bilinen hastalıklar, İlaçlar, Not.

- [ ] **Step 2: DoctorRequestView'i güncelle**

Sorguyu genişlet: request + patient (isim) + category/subcategory/operation_type adları + fotoğraflar (`kind='photo'`) + röntgenler (`kind='xray'`) ayrı imzalı URL listeleri.
Render: `<PatientInfoCard .../>` + **Fotoğraflar** bölümü + (röntgen varsa) **Diş Röntgeni** bölümü + mevcut kabul/red aksiyonları. (AI uyarıları için yorum yer tutucu bırak — M2.)

- [ ] **Step 3: Derle + canlı doğrula**

`npm run build` temiz. Ayşe ile atanmış talebi aç → hasta adı/kategori/operasyon/yaş/cinsiyet/BMI/tıbbi bilgi + fotoğraflar görünür; Diş talebinde röntgen ayrı görünür.

- [ ] **Step 4: Commit**

```bash
git add src/features/requests/PatientInfoCard.tsx src/features/doctor/DoctorRequestView.tsx
git commit -m "feat: doktor görünümü — hasta bilgisi/BMI/tıbbi + foto & diş röntgeni

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: RequestDetail — hasta bilgisi + fotoğraflar (satışçı/oluşturan)

**Files:**
- Modify: `src/features/requests/RequestDetail.tsx`, `src/features/requests/useRequests.ts` (detay sorgusuna foto+isim ekle)

**Interfaces:**
- Consumes: `PatientInfoCard`, `useRequestDetail` (genişletilmiş).

- [ ] **Step 1: useRequestDetail'i genişlet**

`useRequestDetail` sorgusuna patient adı + category/subcategory/operation adı + fotoğraflar (kind='photo') + röntgenler (kind='xray') imzalı URL'leri ekle (DoctorRequestView'daki desenle). Dönen obje: `{ req, responses, patientName, categoryName, subcategoryName, operationName, photos, xrays }`.

- [ ] **Step 2: RequestDetail render**

`<PatientInfoCard .../>` + fotoğraflar + (varsa) röntgen bölümü ekle (durum pill'i ve doktor teklifleri RoleGate'i M1'deki gibi kalır; aracı planı görmez).

- [ ] **Step 3: Derle + canlı doğrula**

`npm run build` temiz. Satışçı ile talep detayında hasta bilgisi + fotoğraflar görünür. Aracı ile: hasta bilgisi + kendi yüklediği fotoğraflar görünür ama doktor planı görünmez (M1 kuralı).

- [ ] **Step 4: Commit**

```bash
git add src/features/requests/RequestDetail.tsx src/features/requests/useRequests.ts
git commit -m "feat: talep detayı — hasta bilgisi + fotoğraflar (satışçı/aracı)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 11: AllRequests — "Doktor atanmadı" rozeti

**Files:**
- Modify: `src/features/admin/AllRequests.tsx`

- [ ] **Step 1: Rozet ekle**

Listede `status === 'submitted'` olan talepler için kırmızı **"Doktor atanmadı"** rozeti göster (StatusPill yanında). Böylece alt-kırılımda doktoru olmayıp takılı kalan talepler görünür ve "Yeniden ata" ile müdahale edilir.

- [ ] **Step 2: Derle + manuel**

`npm run build` temiz. Koordinatör `/admin/requests` → atanmayan talep kırmızı rozetle görünür.

- [ ] **Step 3: Commit**

```bash
git add src/features/admin/AllRequests.tsx
git commit -m "feat: koordinatör panosunda 'Doktor atanmadı' rozeti

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 12: Uçtan uca doğrulama (Playwright güncelleme + canlı)

**Files:**
- Modify: `tests/e2e/core-flow.spec.ts` (yeni zorunlu alanları doldur)

- [ ] **Step 1: E2E'yi yeni alanlara uyarla**

`core-flow.spec.ts` talep oluşturma adımına yaş/boy/kilo/cinsiyet + üç tıbbi alan "Yok" işaretleme ekle (form artık bunları zorunlu tutuyor). Kategori Saç Ekimi (alt-kırılımsız) kalır; Ayşe/Mehmet scope'u backfill'den geldiği için atama çalışır. Kalan akış (doktor kabul → satışçı planı görür → aracı görmez) aynı.

- [ ] **Step 2: E2E çalıştır**

Run: `npm run e2e`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/e2e/core-flow.spec.ts
git commit -m "test: E2E'yi zenginleştirilmiş talep formuna uyarla

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review Notları (spec kapsamı)

- **§2 talep alanları (yaş/kilo/boy/cinsiyet zorunlu, tıbbi Yok)** → Task 1 (validation), Task 3 (şema), Task 4 (kayıt), Task 5 (form). ✅
- **§2.2 diş röntgeni** → Task 3 (photo.kind), Task 4 (upload kind), Task 5 (koşullu yükleyici), Task 9/10 (gösterim). ✅
- **§3.1 çoklu yetkinlik (doctor_scope) + resolveAssignees** → Task 2 (domain), Task 3 (tablo+backfill), Task 4 (atama), Task 7 (UI scope düzenleme). ✅
- **§3.2 kart içeriği (foto/CV/branş/ağırlıklı iş)** → Task 7. ✅
- **§3.3 performans paneli** → Task 7 (useDoctorMetrics). ✅
- **§3.3 giriş hesabı (create-doctor)** → Task 8. ✅
- **§4.1 doktor görünümü** → Task 9. **§4.2 satışçı görünümü** → Task 10. ✅
- **§4.3 atanamayan rozet** → Task 11. **§4.4 navigasyon** → Task 6. ✅
- **§6 domain/test** → Task 1, 2; canlı doğrulamalar Task 8/9/10/12. ✅
- **Kapsam dışı (AI/skor otomasyonu/SLA)** M2/M3'e bırakıldı — doğru. ✅
```
